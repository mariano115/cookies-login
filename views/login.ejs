<!DOCTYPE html>
<html lang="en">

<body>
  <div
    style="background-color: rgb(191, 191, 209); width: 30%;height: auto; display: flex ; justify-items: center; flex-direction: column; align-items: center">
    <div>
      <h1 style="color: blue">Login de Usuario</h1>
      <br />
      <form id="loginFormulario" method="get" action="/">
        <div>
          <div>
            <label for="nombre"><b>Ingrese su nombre</b></label>
          </div>
          <input id="nombre" type="text" name="nombre"/>
        </div>
        <button id="enviar" type="submit" style="margin-top: 10px; margin-bottom: 40px;">Enviar</button>
      </form>
    </div>
    <script src="/socket.io/socket.io.js"></script>
    <script>
      let socket = io()
      /* const loginFormulario = document.getElementById('loginFormulario');
      loginFormulario.addEventListener("submit", (e) => {
        e.preventDefault();
        const nombreLogin = {
          nombre: document.getElementById("nombre").value
        };
        socket.emit("login", nombreLogin);
      }) */
    </script>
  </div>
</body>

</html>